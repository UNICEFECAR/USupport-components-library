import React from "react";
import PropTypes from "prop-types";

import "./email.scss";

/**
 * Email
 *
 * Email template
 *
 * @return {jsx}
 */
export const Email = (
  {
    /* Add props here */
  }
) => {
  const computedHTML = "";
  return computedHTML;
};

Email.propTypes = {
  // Add propTypes here
};

Email.defaultProps = {
  // Add defaultProps here
};
