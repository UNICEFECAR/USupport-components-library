import React from 'react';

import { Like } from './Like';

export default {
  title: 'Components Library/icons/Like',
  component: Like,
  argTypes: {},
};

const Template = (props) => <Like {...props} />;

export const Default = Template.bind({});
Default.args = {};
