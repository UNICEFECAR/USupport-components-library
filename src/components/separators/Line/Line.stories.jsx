import React from "react";

import { Line } from "./Line";

export default {
  title: "Components Library/separators/Line",
  component: Line,
  argTypes: {},
};

const Template = (props) => <Line {...props} />;

export const Default = Template.bind({});
Default.args = {};
