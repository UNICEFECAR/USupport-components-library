import React from 'react';

import { ButtonOnlyIcon } from './ButtonOnlyIcon';

export default {
  title: 'Components Library/buttons/ButtonOnlyIcon',
  component: ButtonOnlyIcon,
  argTypes: {},
};

const Template = (props) => <ButtonOnlyIcon {...props} />;

export const Default = Template.bind({});
Default.args = {};
