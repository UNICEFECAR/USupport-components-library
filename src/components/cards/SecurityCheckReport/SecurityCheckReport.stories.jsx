import React from 'react';

import { SecurityCheckReport } from './SecurityCheckReport';

export default {
  title: 'Components Library/cards/SecurityCheckReport',
  component: SecurityCheckReport,
  argTypes: {},
};

const Template = (props) => <SecurityCheckReport {...props} />;

export const Default = Template.bind({});
Default.args = {};
