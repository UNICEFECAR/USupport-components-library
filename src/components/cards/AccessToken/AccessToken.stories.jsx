import React from 'react';

import { AccessToken } from './AccessToken';

export default {
  title: 'Components Library/cards/AccessToken',
  component: AccessToken,
  argTypes: {},
};

const Template = (props) => <AccessToken {...props} />;

export const Default = Template.bind({});
Default.args = {};
